/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'barkprotocol.app',
        'another-domain.com',
        'ucarecdn.com'
    },
  };
  
  export default nextConfig;
  